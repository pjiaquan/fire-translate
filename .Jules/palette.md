## 2026-09-06 - Add ARIA labels to language selectors
**Learning:** Select elements without explicit `<label>` tags need `aria-label` for screen readers to announce their purpose correctly.
**Action:** Always add `aria-label` to standalone `<select>` dropdowns to improve accessibility.
